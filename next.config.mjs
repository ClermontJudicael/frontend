/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['localhost', 'localhost:5000'], // Autorise les images provenant de localhost et du port 5000
    },
  };
  
  export default nextConfig;
  